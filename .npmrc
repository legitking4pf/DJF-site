# Ensure dependencies are hoisted so the build plugin can find them
public-hoist-pattern[]=*semver*
public-hoist-pattern[]=*@netlify/plugin-nextjs*

# Flatten the node_modules structure to avoid deep symlink issues
shamefully-hoist=true

# Compatibility for Next.js standalone tracing with pnpm
node-linker=hoisted
